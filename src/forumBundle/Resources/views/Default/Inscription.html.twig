{% extends('base.html.twig') %}
{% block content %}
    <section class="clearfix pageTitleSection" style="background-image: url({{ asset('') }});">
        <div class="container">
            <div class="row">
                <div class="col-xs-12">
                    <div class="pageTitle">
                        <h2>Inscription Page</h2>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <section class="clearfix signUpSection">
        <div class="container">
            <div class="row">
                <div class="col-sm-8 col-xs-12">
                    <div class="signUpFormArea">
                        <div class="priceTableTitle">
                            <p>Veuillez remplir les champs ci-dessous pour créer votre compte. Nous enverrons les informations de votre compte à l'adresse e-mail que vous entrez. Votre adresse e-mail et vos informations ne seront pas vendues ou partagées avec des tiers. Si vous avez déjà un compte, s'il vous plaît, <a href="">Se connecter</a>.</p>
                        </div>
                        {{ form_start(form) }}
                            <div class="formSection">
                                <h3>Informations du compte</h3>
                                <div class="row">
                                    <div class="form-group col-xs-12">
                                        {{ form_label(form.email) }}
                                        {{ form_widget(form.email) }}
                                    </div>
                                    <div class="form-group col-xs-12">
                                        {{ form_label(form.username) }}
                                        {{ form_widget(form.username) }}
                                    </div>
                                    <div class="form-group col-sm-6 col-xs-12">
                                        {{ form_label(form.plainPassword.first) }}
                                        {{ form_widget(form.plainPassword.first) }}
                                    </div>
                                    <div class="form-group col-sm-6 col-xs-12">
                                        {{ form_label(form.plainPassword.second) }}
                                        {{ form_widget(form.plainPassword.second) }}
                                    </div>

                                </div>
                            </div>
                            <div class="formSection">
                                <div class="row">
                                    <div class="form-group col-xs-12 mb0">
                                        {{ form_widget(form.Submit) }}
                                    </div>
                                </div>
                            </div>
                        </div>
                        {{ form_end(form) }}
                    <br><br><br><br>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}